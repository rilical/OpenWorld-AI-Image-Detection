seed: 123
deterministic: true

data:
  dataset: "diffusiondb"
  batch_size: 64
  num_workers: 0
  img_size: 224
  diffusiondb_subset: "2m_first_1k"
  max_eval_samples: 1000

eval:
  save_predictions: true

output:
  root: "outputs/runs"
