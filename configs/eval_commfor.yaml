seed: 123
deterministic: true

data:
  dataset: "commfor_small"
  batch_size: 64
  num_workers: 2
  img_size: 224
  calibration_fraction: 0.1
  split: "validation"

eval:
  save_predictions: true

output:
  root: "outputs/runs"
